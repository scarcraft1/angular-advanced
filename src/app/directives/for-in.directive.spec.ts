import { ForInDirective } from './for-in.directive';

describe('ForInDirective', () => {
  it('should create an instance', () => {
    const directive = new ForInDirective();
    expect(directive).toBeTruthy();
  });
});
