import { HotCounterDirective } from './hot-counter.directive';

describe('HotCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new HotCounterDirective();
    expect(directive).toBeTruthy();
  });
});
